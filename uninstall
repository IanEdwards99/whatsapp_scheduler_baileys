#!/bin/bash
# Uninstall WhatsApp Scheduler (removes deps + session; keeps schedules unless you confirm)
set -e
DIR="$(cd "$(dirname "$0")" && pwd)"

if [ -f "$DIR/venv/bin/python" ]; then
    "$DIR/venv/bin/python" "$DIR/manage.py" uninstall "$@"
else
    python3 "$DIR/manage.py" uninstall "$@"
fi
